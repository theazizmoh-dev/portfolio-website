
import { postSchema, authorSchema, projectSchema, serviceSchema, blockContent } from '../../src/sanity/schemas.js';

export const schemaTypes = [
    postSchema,
    authorSchema,
    projectSchema,
    serviceSchema,
    blockContent
]
